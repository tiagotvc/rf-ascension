type BrandProps={href?:string;admin?:boolean;className?:string};
export default function Brand({href="/",admin=false,className=""}:BrandProps){return <a href={href} className={`brand-link ${className}`} aria-label={admin?"RF Ascension — painel administrativo":"RF Ascension — início"}><span className="brand"><span className="brand-mark">RF</span><span className="brand-copy"><strong>ASCENSION</strong><small>{admin?"ADMIN CONSOLE":"PRIVATE SERVER"}</small></span></span></a>}
